{
  "name": "withings-weight-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-vendor": "node scripts/sync-vendor.mjs",
    "check-dashboard": "node --check src/dashboard/app.js && node --check src/dashboard/shared.js && node --check src/dashboard/meals.js && node --check src/dashboard/exercise.js && node --check src/dashboard/sw.js",
    "dev": "npm run sync-vendor && wrangler dev",
    "deploy": "npm run sync-vendor && wrangler deploy",
    "test": "npm run sync-vendor && vitest run",
    "typecheck": "npm run sync-vendor && tsc --noEmit && tsc --noEmit -p test/tsconfig.json && npm run check-dashboard",
    "verify": "./scripts/scan-real-values.sh && npm run typecheck && npm test",
    "test:e2e": "npm run sync-vendor && node e2e/setup-db.mjs && playwright test"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3",
    "@hono/mcp": "^0.3.1",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "chart.js": "4.4.9",
    "hono": "^4.6.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@cloudflare/workers-types": "^5.20260730.1",
    "@playwright/test": "^1.62.1",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0",
    "wrangler": "4.122.0"
  }
}
